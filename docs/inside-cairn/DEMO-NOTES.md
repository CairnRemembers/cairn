# Recording notes

These demonstrations use recordings from a local Cairn installation made in September 2026. The exact running commit was not captured, so parity with release 0.3.3 is unverified. Consult the setup documentation for your installed version.

Private names, queries and excerpts use illustrative substitutions. Original memory kinds, counts, timestamps, tool metadata and model/human attribution are retained. Project controls remain visible; no project action is performed in the recording.

Live Feed combines an earlier tool event with a separate recording of model and human entries. It does not demonstrate a new cross-assistant handoff. Its source region is 250 × 338 pixels, so some original text is small and low-contrast.

Index shows a six-second excerpt of the recorded A section with example AI and memory topic names. Counts, row structure and controls remain as recorded. The opening and closing holds and scroll timing are edited. The clip does not demonstrate typing a filter or selecting a topic.

Connections shows five recorded hover positions on the original graph. Tooltip excerpts use examples; memory kinds and dates remain as recorded.
