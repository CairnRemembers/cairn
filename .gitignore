__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/
.cairn/
*.db
*.db-shm
*.db-wal
*.bak
_atlas*.png
output/
hook_debug.log
.claude/settings.local.json
.claude/launch.json
.claude/run-demo-twin.bat
.dashboard.restart.log

# local research + personal distilled claims — NEVER commit (privacy, not code)
experiments/
*-claims.json
p4_*.json

# internal-only docs + maintainer/seed scripts — kept LOCAL, never in the repo.
# Maintainer working notes and personal seed data — nothing a user needs to
# install or use Cairn.
CLAUDE.md
HANDOFF*
SETUP.md
CAIRN_V2*
*.patch
scripts/seed_test_data.py
scripts/backfill_knowledge.py
